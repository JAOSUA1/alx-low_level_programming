#include "main.h"

/**
 * add - compute addition of two numbers
 * @g: inputed value to add
 * @h: second value to be added
 *
 * Return: Always result on success
 */
int add(int g, int h);
{
	return (g + h);
}

#include "main.h"

/**
 * main - prints the name of the program
 * @argv: array of command line input
 *
 * Return: Always 0 on success
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	_putchar("%s\n", argv[0]);
	return (0);
}
